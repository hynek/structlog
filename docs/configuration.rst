Configuration
=============

For your convenience, it is possible to define default values for both the processor chain as well as the context carrying class.
Nevertheless, it's possible to overwrite those defaults on a per-logger basis.
